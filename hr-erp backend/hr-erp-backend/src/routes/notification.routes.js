const express = require('express');
const router = express.Router();

// Placeholder - később implementálva lesz
router.get('/', (req, res) => {
  res.json({
    success: true,
    message: 'Notification endpoint - később implementálva'
  });
});

module.exports = router;
